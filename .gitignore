# dépendances
/node_modules

# build
/.next/
/out/
/build

# tests
/coverage
/playwright-report/
/test-results/
/blob-report/
/playwright/.cache/

# TypeScript
*.tsbuildinfo
next-env.d.ts

# secrets et environnement — invariant I9
.env
.env.*
!.env.example
*.pem

# divers
.DS_Store
*.log
.vercel

# stockage d'objets local (BON-2, lib/documents/stockage.ts) — dos-d'âne en
# attendant les identifiants S3-compatibles de l'hébergeur (L0-09, L8-05).
# Jamais versionné : I9 interdit toute photo réelle dans le dépôt.
/.donnees-locales/

# contrôle post-migration — inventaire produit par le workflow, jamais versionné
/inventaire-controle.json

# fichier réel apporté par l'exploitation — invariant I9, ne doit JAMAIS entrer au dépôt
Suivi_SAV_CODIMA*.xlsx
