# Registre — la nuit du 14 septembre 2026

*Une entrée par unité de travail. La mesure d'abord, la prose ensuite.*

*Point de départ : `main` = `5e39d5b` (constaté par `git log`, pas de mémoire),
arbre propre. **Cinq branches, une par ticket**, et aucune fusion — un lot
fusionné pendant la nuit déclencherait une migration automatique (D116) que
personne ne surveille.*

*Horloge du conteneur : `date -u` → **2026-09-14 11:10 UTC** au démarrage,
**13:0x UTC** à la rédaction. Les documents suivent le calendrier de
l'exploitation ; les mesures portent l'heure lue.*

---

## CE QUI EST POUSSÉ, ET OÙ

| Proposition | Branche | Ce que c'est |
|---|---|---|
| **#183** | `claude/stoic-curie-a1i3qq-r3-13` | régler les jours travaillés et les plages d'un calendrier |
| **#184** | `claude/stoic-curie-a1i3qq-r3-14` | déclarer une absence, et fermer l'autre bout de RG-PLA-06 |
| **#185** | `claude/stoic-curie-a1i3qq-r3-15` | le catalogue des prestations reçoit un dépôt, des routes et un écran |
| **#186** | `claude/stoic-curie-a1i3qq-r3-12` | la marque `LIVRÉ` se pose sur un CHEMIN, et un gardien le mesure |
| **#187** | `claude/stoic-curie-a1i3qq-tickets` | la file écrite : lots 4 et 5, le métier, D118, les captures |

> **L'ORDRE DE FUSION N'EST PAS INDIFFÉRENT.** #183, #184, #185 sont
> indépendantes. **#186 les contient** — son gardien mesure l'état réel du
> dépôt, et sa liste d'exemptions a été écrite sur l'arbre qui porte les trois.
> Fusionnée seule, elle rougirait sur les fonctions que R3-14 et R3-15 viennent
> d'ouvrir. **#187 est posée sur #186.** *Une fois les précédentes fusionnées,
> chaque diff se réduit à son propre commit.*

**Deux propositions touchent `prisma/migrations/`** — #183
(`20260914100000_plages_reglables_r3_13`) et #184
(`20260914110000_absence_reglable_r3_14`). D116 les porte à la base de
**démonstration** automatiquement à la fusion ; `prisma/seed.ts` n'est pas
touché, **aucun geste de main n'est dû**.

## CE QUI EST VERT, ET AVEC QUELLE COMMANDE

`pnpm verify` — `format:check`, `typecheck`, `lint`, unitaires, isolation,
`build` — **sur chacune des cinq branches**, jouée à chaque fois sur l'état
poussé. Décomptes de la dernière : **1 847 scénarios unitaires, 884
d'isolation**.

**`pnpm verify:full` N'A PAS TOURNÉ**, et c'est écrit plutôt que tu (§11 du
protocole). Les scénarios Playwright exigent une base semée, un serveur bâti et
un mot de passe choisi à la main ; *ce qui a été mesuré à la place est `verify`
entier, six commandes, à chaque fois.* **Les deux contrôles datés —
`feries:horizon` et `audit:partitions` — n'ont pas été joués non plus.**

## LES ARBITRAGES QUE J'AI TRANCHÉS SEULS

*Tous relèvent de la session (§1 du protocole) : aucun ne touche l'argent
facturé, une obligation légale ou ce qu'un client voit. Chacun porte sa
condition de réouverture.*

| | |
|---|---|
| **R3-13 (1)** | un changement d'horaires ne touche RIEN de ce qui est posé ; ce qui se recalcule est **dit** à l'écran |
| **R3-13 (2)** | le chevauchement de plages est refusé **par la base** ; deux plages qui se touchent ne se chevauchent pas |
| **R3-13 (3)** | le pas tombe dans la plage **dans les deux sens** — raccourcir une plage ET élever le pas |
| **R3-15 (1)** | **aucune saisie de checklist** : personne n'a dit ce que porte la colonne, et l'inventer la figerait pour toutes les sociétés |
| **R3-12** | le jugement `LIVRÉ`/`BLOQUÉ` de neuf tickets — **un seul bascule**, L8-07 |

## LES ARBITRAGES QUE JE LAISSE — et QUATRE sont PROVISOIRES

**Les quatre décisions de R3-14 ont été rendues par l'exploitation de nuit, PAS
par Alexis.** Elles sont inscrites au ticket comme telles, elles sont
**réversibles**, et chacune dit comment se rouvrir.

> **La plus importante : une absence ne porte AUCUNE nature.** La colonne
> existait déjà — une énumération `NOT NULL` dont `arret` est un arrêt de
> travail, *c'est-à-dire une donnée de santé sur un salarié nommé, en clair*.
> **Mesuré avant d'agir : zéro ligne en base.** La colonne et le type restent,
> **dormants** ; un déclencheur refuse toute écriture qui en porterait une.
> *Ratifier dans un sens, c'est retirer le déclencheur ; dans l'autre,
> supprimer les colonnes — et ce geste reste gratuit tant qu'aucune ligne n'en
> porte.*

**Et trois questions nouvelles partent à l'arbitrage :**

| Ticket | La question | Pourquoi elle n'est pas à moi |
|---|---|---|
| **R3-16** | changer les horaires d'une agence change le **supplément d'une intervention déjà facturée** | c'est de l'argent facturé |
| **R4-04** | laquelle des **deux structures de menu** — devant l'image | c'est ce que l'exploitation voit tous les jours |
| **R5-02** | un temps **mesuré** entre-t-il tel quel dans la facture, ou un temps **saisi** le confirme-t-il ? | *la différence finit sur une facture* |
| **L4-09** | le **coût interne** d'une heure de technicien | c'est une donnée de paie |

## L'ÉTAT DE LA FILE

**Avant** : 4 `LIBRE`, 115 `LIVRÉ`, 34 `BLOQUÉ`.
**Après** : **7 `LIBRE`, 119 `LIVRÉ`, 51 `BLOQUÉ`** — 177 tickets.

*Les sept libres, dans l'ordre du document* : **L1-03b**, **L1-05b**,
**L2-06b**, **L4-01** — que `pnpm file` nomme en premier —, **R4-02**,
**R4-03**, **R5-01**.

> **La file a grandi de 27 tickets et elle est plus HONNÊTE, pas plus longue.**
> Les 17 nouveaux bloqués ne bloquent pas parce qu'on les a écartés : ils
> bloquent parce qu'on a mesuré ce qui leur manque. *Un lot sans tickets
> ressemble à un lot qui attend.*

## CE QUE JE N'AI PAS FAIT, ET POURQUOI

| | |
|---|---|
| **`pnpm verify:full`** | Playwright exige une préparation manuelle ; `verify` entier a été joué à la place, et c'est dit |
| **Les 46 paires de captures `clair`/`sombre` identiques** | l'ordre de la nuit l'interdisait : *il n'y a pas d'apparence sombre, et c'est une absence décidée* |
| **Le portail inatteignable** | même raison : c'est un arbitrage d'exploitation, pas un ticket. Les 4 captures du portail restent refusées, et le refus le dit |
| **Toucher à `lib/navigation/entrees.ts`** | l'ordre était de PROPOSER, pas d'imposer. Les trois gardiens n'ont pas bougé d'une ligne |
| **Construire quoi que ce soit des lots 4 à 7** | *rendre une file de travail écrite vaut mieux qu'un écran de plus* |
| **Le second écran des demandes, les contacts, les familles** | trois tickets écrits (L1-03b, L1-05b, L2-06b), aucun pris : la nuit avait déjà ses quatre |

## TROIS DÉFAUTS RENCONTRÉS EN CHEMIN, corrigés plutôt que tus

1. **L'en-tête du backlog renvoyait à `R1-02`** pour porter le travail de R3-12.
   R1-02 est le README des captures, livré le 12/09. *Une règle qui renvoie au
   mauvais ticket envoie chercher là où il n'y a rien* — c'est la même faute que
   l'entrée de barre qui nommait `L1-09` au lieu de `L1-11`.
2. **La mesure « neuf modules sans chemin » avait vieilli en quatre jours** : il
   en reste **quatre**. *Une liste écrite à la main vieillit ; une commande ne
   vieillit pas.*
3. **Un code d'épreuve fabriqué depuis les huit PREMIERS caractères d'un UUID v7
   rendait deux fois la même valeur** — l'horodatage est sur les bits de poids
   fort (I10). *C'est le §9 du 14/09 sur le repli du planning, rencontré une
   seconde fois le même jour, dans un fichier de scénarios.*

## OÙ REPRENDRE

**Rien n'est en cours.** Les cinq branches sont poussées et vertes, aucune n'est
fusionnée. La reprise se fait par `pnpm file`, qui nomme **L4-01**.

*Si Alexis veut d'abord voir plutôt que lire : `docs/propositions/navigation.html`
et ses deux captures, puis `docs/captures/calendrier-plages--clair--1280.png`,
`absences--clair--1280.png` et `prestations--clair--1280.png`.*
