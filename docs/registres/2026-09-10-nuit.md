# Registre — nuit du 10 au 11 septembre 2026

*Une entrée par unité de travail. La mesure d'abord, la prose ensuite. Écrit à
chaud, commité et poussé après chaque unité : si la session s'arrête juste
après un commit, une session neuve doit savoir où on en est et pourquoi, sans
rien demander.*

*Point de départ : `main` = `2cf99fa`, arbre propre, aucune PR ouverte, aucune
migration en attente. Reprise depuis `docs/registres/2026-09-09-suite.md`, §8.
Horloge lue au départ : `2026-09-09 07:02 UTC` (18 h 02 à Nouméa).*

---

## 1 — LA RÉÉMISSION DU JETON D'AMORÇAGE *(votre point 1 « en vol »)*

### Ce que la mesure a dicté AVANT d'écrire, et qui n'était pas dans le plan de la veille

Le plan de la veille disait : *« il refuse dès que l'identité visée porte une
ligne de `compte`, c'est-à-dire dès qu'un mot de passe a été posé une fois »*.
**La prémisse est fausse, et je l'ai lue plutôt que crue** : `signUpEmail`
crée la ligne de `compte` **avec l'empreinte du mot de passe jetable**. Une
identité fraîchement amorcée porte donc DÉJÀ une ligne de `compte`, et le
cliquet tel qu'écrit aurait refusé toujours.

| Ce que j'ai regardé | Ce que ça rend |
| --- | --- |
| `better-auth/dist/api/routes/sign-in.mjs:326` | `currentPassword = credentialAccount.password ; if (!currentPassword) → INVALID_EMAIL_OR_PASSWORD` — **une empreinte nulle refuse la connexion** |
| `better-auth/dist/api/routes/password.mjs:165-171` | `resetPassword` : compte trouvé → `updatePassword` ; **n'exige pas que l'empreinte existe** |
| `compte_modification` (migration L1-02d) | `UPDATE` admis sous désignation par `utilisateur_id` — le geste peut effacer l'empreinte sans nouvelle politique |
| candidats écartés | `derniere_connexion` : **personne ne l'écrit** ; `journal_acces` : une trace n'est pas un verrou ; `session` : effacée à l'expiration ; `modifie_le` : bouge pour d'autres raisons ; `verification` par `valeur` : **illisible** — désignée par l'identifiant opaque seul |

**Le fait retenu : `compte.mot_de_passe IS NULL`.** L'amorçage **efface
désormais l'empreinte jetable** après l'inscription (décompte exigé à 1) ; la
première réinitialisation par jeton en écrit une, et l'état ne revient jamais.
C'est le seul fait à la fois lisible par le geste et irréversible par
construction. **C'est une modification du geste d'amorçage, et je la signale
comme telle** : elle ne touche ni politique, ni rôle, ni droit — elle retire un
moyen de connexion que personne n'avait choisi. À ratifier (D65, complément).

### Ce qui est construit

- `reemettreJetonPremierAcces` dans `lib/auth/amorcage.ts` ; `--reemettre`
  dans le même script ; `reemission_premier_acces` dans `EvenementAcces`
  (migration `20260910000000_reemission_premier_acces`, une ligne de DDL,
  **aucune politique**).
- Refus lisibles : identité inconnue, société inconnue, identité non habilitée
  sur la société, aucune ligne de `compte`, **un mot de passe existe**.
- Il ne rouvre jamais `utilisateur_ouverture` (mesuré : la société reste
  fermée à une nouvelle identité après réémission), ne laisse aucune session,
  trace dans `journal_acces`.

### Les mesures

| Scénario (`tests/isolation/amorcage-premier-compte.test.ts`) | Résultat |
| --- | --- |
| TÉMOIN — l'amorçage laisse `mot_de_passe = NULL` | vert |
| l'identité sans mot de passe ne se connecte avec rien | vert |
| réémet, trace `ouverture_identite` puis `reemission_premier_acces`, 0 session | vert |
| le jeton réémis ouvre le compte ; **refus mesuré APRÈS le succès, même identité** | vert |
| JUMEAU — empreinte mise de côté, fait remis (`NULL`), la réémission REPASSE ; empreinte rendue, le refus revient | vert |
| identité inconnue, identité étrangère à la société | vert |
| ne rouvre jamais le chemin d'ouverture | vert |

**Vu tomber :** la condition `mot_de_passe !== null` neutralisée dans le
code, **2 scénarios rougissent** (« le cliquet CLAQUE » et le jumeau), 16
restent verts. Remise en place, 18/18.

### Ce que le geste ne sait pas faire, écrit plutôt que tu

Invalider un jeton précédent encore vivant. `verification` ne se lit que par
l'identifiant opaque présenté, et le geste ne l'a pas gardé. Deux jetons
peuvent être valides pendant l'heure du premier — la fenêtre existait déjà.

### Documents

Note de mise en ligne (5.1 second encadré barré et remplacé, étapes 1 et 12),
README, D65 complément, CLAUDE.md §6.

---

## 2 — `scripts/taux-initial.mts` : LE PREMIER TAUX, GESTE SÉPARÉ *(votre point 3 « en vol » — « Livrez »)*

### Ce qui est construit

`lib/tarification/taux-initial.ts` et `scripts/taux-initial.mts`
(`TAUX_INITIAL_CONFIRME=oui`, `--societe`, `--montant`, `--date` facultative).

| Propriété | Comment elle est tenue |
| --- | --- |
| **une seule ligne, la première** | `count()` sous le contexte de la société ; > 0 ⇒ refus lisible, rien n'est écrit |
| **la devise est celle de la société** | lue sur `societe.devise`, jamais un argument — le déclencheur de L1-07 refuserait l'autre de toute façon |
| **la date par défaut est le jour du geste, DANS LE FUSEAU DE LA SOCIÉTÉ** | `maintenant(lireFuseau(societe.fuseau_horaire))` — L0-08, aucune date courante sans fuseau |
| **ni montant ni date codés** | tous deux fournis ; le script refuse un montant non entier et RÉPÈTE le montant formaté (`formatMoney`) pour qu'une erreur d'échelle se voie |
| **rejouable sans dégât** | le second appel refuse, lisiblement |

`argument()` a quitté le script d'amorçage pour `scripts/lib/arguments.ts` :
le geste de tarif en avait besoin, et une seconde copie aurait été une liste
recopiée « pour la lisibilité » (§9, 01/09).

### Les mesures (`tests/isolation/taux-initial.test.ts`, 7 scénarios)

| Scénario | Résultat |
| --- | --- |
| TÉMOIN — la table naît vide | vert |
| une ligne, XPF, `2026-10-01` ; `tauxEnVigueur` : 7 000 le 01/10, `null` le 30/09 | vert |
| second appel sur la MÊME société refusé, **après** le succès ; novembre non écrit | vert |
| B n'est pas concernée par le refus de A, et pose le sien en EUR (2 décimales) | vert |
| sans date : le jour courant dans `Pacific/Noumea` | vert |
| montant nul, société inconnue, date `01/10/2026` refusés | vert |
| JUMEAU — ligne retirée, le geste REPASSE ; rendue, le refus revient | vert |

**Vu tomber :** la condition `existants > 0` neutralisée, **2 scénarios
rougissent** (le second appel, le jumeau). Remise en place, 7/7.

### Une trouvaille en passant, inscrite pour l'hygiène du plan

Le parseur des décisions (`scripts/lib/cablage-arbitrages.ts`,
`lireDecisions`) **ferme une section au premier sous-titre** `###` qui n'est
pas `Dnn` : le corps de D65 s'arrête avant « ### L'impasse… », celui de D68
avant « ### Ce que la nuit… ». L'empreinte d'un ticket ne couvre donc que le
préambule de chaque décision citée — mesuré : le complément ajouté à D68 sous
un `###` n'a réveillé aucune estampille. *Un gardien qui regarde le début
d'un texte et se croit devant le texte.* À traiter avec le gardien du point 4,
qui touche ce parseur.

---

## 3 — LA MESURE PRÉALABLE DE A, ET LES SIX DÉCISIONS A–F *(inscrites, rien construit)*

### Ce que les trois tables portent DÉJÀ — lu au schéma, pas au cahier

| Table | Porte | Ne porte pas |
| --- | --- | --- |
| `calendrier` | un jeu d'horaires NOMMÉ par société ; partagé (Ducos + Dolbeau = `DEMO-NOUMEA`) | ni horaire, ni personne |
| `calendrier_plage` | les horaires hebdomadaires — jour ISO, minutes locales, plusieurs plages par jour ; un jour sans plage est fermé | aucune date (récurrence) |
| `calendrier_ferie` | les jours travaillés ou chômés PAR AGENCE — férié travaillé, pont ; unique (agence, date), chaîné au territoire | aucun horaire |
| `agence.calendrier_id` | le rattachement — **nullable** | — |
| `lib/calendar/usages.ts` | `conflitPose(calendrierTravailTechnicien, …)` — D13 | **ce paramètre n'a AUCUNE source au schéma** |

**Conclusion : le paramétrage par agence existe depuis le lot 0, rien à
ajouter pour lui.** Ce qui manque, mesuré : **(1) le technicien n'a pas
d'agence** — `utilisateur_societe` = `utilisateur_id`, `societe_id`, `role`,
et c'est tout, alors que le CLAUDE.md (I1), D12 et D13 affirment le contraire
et que le chapitre 11 prévoit une table **`technicien`** (utilisateur,
société, **agence**, coût horaire, véhicule, actif) qui n'existe pas ;
**(2) aucune exception par technicien**, ni table ni colonne.

*Trois documents affirment un état que le schéma n'a pas — la pente du 07/09,
sur la constitution elle-même.*

### Ce qui est inscrit (D72 à D76, et F)

- **D72** — journée, créneau, horaires par agence (existants) **avec exception
  par technicien** : *forme proposée par la session, à ratifier* —
  `technicien.calendrier_id` nullable, un jeu d'horaires de plus dans
  `calendrier`, fériés et ponts toujours ceux de l'agence. **Règle de priorité
  écrite**, et ce qu'elle fait sans exception : exactement le calendrier de
  l'agence ; agence sans calendrier ⇒ refus de poser (I7).
- **D73** — refus nommé ; la distinction avec D50 écrite : *le refus dit tout
  ce que son destinataire peut lire, rien de ce qu'il ne peut pas.*
- **D74** — trajet = forfait ; `site.temps_trajet_min` **donnée de
  planification et rien d'autre**, écrit aux endroits où on la lira : RG-PLA-05,
  RG-INT-07, le schéma, `COMMENT ON COLUMN` (migration `20260910010000`, un
  commentaire, rien d'autre), `lib/sites/depot.ts`, l'aide du champ.
- **D75** — écran Sites, ticket **L3-16**.
- **D76** — taux d'occupation, formule au dictionnaire
  (`vocabulaire.taux_occupation.*`), gardien contre « productivité »
  (`tests/unit/i18n/taux-occupation.test.ts`), ticket **L3-17**, dépend de D72.

### F — ce qui contredit, et ce qui ne contredit pas

| | Verdict |
| --- | --- |
| D72 ↔ narratif « semaine par défaut » | non normatif (D1) ; la journée l'emporte |
| D72 ↔ D13, I7, RG-PLA-01/02 | aucune contradiction — D13 nommait déjà le calendrier du technicien sans lui donner de source |
| **D74 ↔ RG-INT-07** | **OUI** — « selon la règle du contrat… » laissait facturer le trajet au temps par contrat ; **amendée**, câblée (`CABLAGE_ATTENDU` : D74 ↔ RG-INT-07, D74 ↔ RG-PLA-05) |
| D74 ↔ D11, D12 | aucune — D11 disait déjà « non facturé au temps » |
| D76 ↔ cahier | aucune — le cahier dit déjà « taux d'occupation » |

**Question que F fait apparaître :** l'exception par technicien (D72)
s'applique-t-elle à l'**assiette de la majoration** (D12/D13 : agence du
technicien) ? La session lit que non. À trancher avant L2-09.

### Effet sur le backlog, mesuré par le gardien

L2-07 réveillé par RG-INT-07 (rien n'y a cessé d'être vrai ; ré-estampillé) ;
L3-01, L3-02, L3-05 amendés ; L3-16 et L3-17 créés ; cinq estampilles
calculées par le gardien lui-même. *Une trouvaille en chemin :* un `---` posé
avant le nouveau titre avait fait entrer une ligne dans le corps de D71 et
réveillé L2-02 — retiré, L2-02 se rendort. **Le corps d'une décision s'étend
jusqu'au titre suivant, séparateur compris.**

### Ce qui est appliqué à la base

Le travail « DB migrate & seed » **n°37** a appliqué
`20260910000000_reemission_premier_acces` sur `af4d28b` — *success*,
07:24:08 → 07:25:57 UTC.
