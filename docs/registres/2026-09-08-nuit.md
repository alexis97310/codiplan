# Registre de la nuit du 8 au 9 septembre 2026

*Écrit au fil de la nuit, poussé après chaque unité de travail. Il se lit sans
cette session : une session neuve doit savoir où on en est et pourquoi, sans
rien demander à l'exploitation.*

**Protocole de la nuit :** on n'attend plus sur un cas d'arrêt — on mesure, on
inscrit la question au registre, et on passe au ticket suivant. Ce qui ne change
pas : *aucune décision n'est prise à la place de l'exploitation* sur le
cloisonnement, les droits, l'argent, une migration déjà appliquée, ou une
question métier qui dépend de comment CODIMA travaille.

---

## 0 — L'état au départ de la nuit

`main` est à `f9c4c0f`. Ce qui vient d'être livré dans la journée :

| Livré | Migration appliquée | Mesure |
| --- | --- | --- |
| **L2-11** — l'impasse multi-société, constatée et nommée | — | 2 scénarios, témoin de non-vacuité |
| **`docs/mise-en-ligne.md`** — la note de déploiement | — | 4 affirmations mesurées, dont « la construction n'exige aucun secret » |
| **L1-02g** — le plancher du second facteur (D64) | **oui**, run #27, toutes étapes vertes | 10 codes comparés puis `429` ; 3 gardiens vus tomber |
| **L1-02g (suite)** — la note de mise en ligne suit le dépôt | — | — |

**Ce qui bloquait au départ, et que l'exploitation a tranché pour la nuit :**

- **L1-05** — D4 se contredisait. *Tranché : famille et modèle deviennent des
  tables métier cloisonnées ; le schéma « référentiel + copie masquante » est
  RETIRÉ.* Construit cette nuit.
- **Tarification (L1-06, L1-07)** — *mécanisme tranché* : tables métier
  cloisonnées, montants entiers dans les types du module monétaire, taux horaire
  **historisé par date d'effet**. Les **valeurs** restent à l'exploitation.
- **Le geste d'ouverture du premier compte** — *à MESURER et PROPOSER, pas à
  poser.* C'est le vrai bloquant : aucun compte de la base hébergée n'a de mot de
  passe.
- **L7-04** — inconstruisible tant qu'aucun `admin_societe` ne peut se connecter.

---

## 1 — Ce qui a été fusionné cette nuit, et appliqué

### L1-05 — familles et modèles : un mécanisme RETIRÉ, pas arbitré

**Ce que le backlog disait, mot pour mot :** *« Familles et modèles — `societe_id`
nullable pour les référentiels de plateforme [D4] ; une copie portant un
`societe_id` masque l'original. Forme de politique : référentiel — lecture
ouverte, écriture aux seuls rôles éditeur —, et RLS activée sans être forcée. »*

**Ce qui a été construit :** `famille_materiel` et `modele_materiel`, tables
métier cloisonnées — `societe_id NOT NULL`, forme « société », RLS forcée,
auditées —, chaînées par une clé composite `(societe_id, famille_id)`. Plus un
module d'entrée Zod `lib/materiel/`, **sans aucune énumération**.

**Ce qui a été MESURÉ, et qui n'était pas prévu :**

| Mesure | Résultat |
| --- | --- |
| les deux tables entrent au périmètre d'audit | **toutes seules** — le scénario « les quatorze tables auditées » a rougi en les nommant, aucune liste d'admis touchée (D55 en acte) |
| référentiels de plateforme portant encore une colonne `societe_id` | **zéro** — `devise`, `parite`, `jour_ferie` n'en ont pas |

**Le second est une DIVERGENCE avec la constitution, et elle est écrite.** I1 dit
« `societe_id` NULL **ou pas de `societe_id` du tout** ». Après ce retrait, la
première moitié n'a plus **aucun exemplaire** : la branche du gardien qui la
reconnaît survit à son dernier cas. Elle est conservée — c'est elle qui rend la
phrase vraie — et un **témoin** dit qu'elle ne garde plus rien de réel, pour que
personne ne la lise comme la preuve qu'un cas existe.

**Gardiens vus tomber**, sur des ruptures réelles :

- le périmètre d'audit inversé, qui a **réclamé les deux tables de lui-même** ;
- le gardien de cohérence du backlog, qui a nommé **les trois tickets citant D4**
  (L0-03, L0-04, L1-05) et exigé leur relecture. **L0-04 avait effectivement
  cessé d'être vrai** : son critère d'acceptation énumérait les tables partagées
  et citait les trois qui viennent de partir. Corrigé, barré, non effacé ;
- le gardien de forme RLS et la clôture de `force-rls`, qui ont refusé les deux
  tables tant qu'elles n'étaient pas rangées ;
- le **jumeau** de la clé composite : la société retirée de la clé, un modèle
  atteint la famille d'une autre société — 1 ligne écrite, décomptée.

**Deux collisions de fixtures, mesurées et réparées à leur source.** Le scénario
des rôles éditeur écrivait dans `modele_materiel` ; reporté sur `jour_ferie`, il
est entré en collision avec `journal-audit.test.ts` (même couple territoire/date)
puis avec `calendriers.test.ts` (qui énumère les territoires exhaustivement). *La
base jetable est partagée par toute la suite* : le scénario **reprend désormais
ce qu'il a écrit**, plutôt que de coupler un voisin à sa population.

`pnpm verify` **exit 0** : 792 scénarios unitaires, 398 d'isolation.

---

## 2 — Ce qui a divergé

*(rien encore)*

---

## 3 — Les questions inscrites au lieu d'être tranchées

### Q1 — LE GESTE D'OUVERTURE DU PREMIER COMPTE

*Demandé : mesurer et proposer, ne pas poser. Voici la mesure, puis la
proposition. **Rien n'a été construit.***

#### Ce qui a été mesuré

| Question | Mesure |
| --- | --- |
| qui appelle `signUpEmail` en production ? | **personne** — deux mentions dans des commentaires, aucun appel hors `tests/` |
| la création d'une identité laisse-t-elle une trace ? | **aucune.** `utilisateur` ne porte **pas** de déclencheur d'audit : elle est la 4ᵉ catégorie de I1, hors du périmètre inversé de D55. Les seize tables auditées ne l'incluent pas |
| et son habilitation ? | **oui** — `utilisateur_societe` est auditée. *La trace existe pour le droit accordé, jamais pour l'identité créée* |
| `journal_acces` peut-il porter le geste ? | **non en l'état** : `EvenementAcces` ne connaît que `bascule_societe`, `bascule_refusee`, `requete_consolidation` |
| qu'exige la base pour ouvrir une identité ? | `utilisateur_ouverture` : `app.societe_id` **non nul** ET `app_peut_administrer_identites()` |
| qu'est-ce qui refuserait un second usage ? | `utilisateur.email` est `@unique` — le refus existe déjà en base |
| précédent d'un geste borné dans le dépôt | `scripts/purge-demonstration.mts` : variable de confirmation explicite `PURGE_DEMONSTRATION_CONFIRMEE=oui`, sans laquelle le script refuse |

#### LE POINT QUI FAIT QUE JE NE POSE PAS

La politique exige une société active **et** un rôle qui administre. Pour le
**premier** compte, il n'existe personne à être : un script d'amorçage devrait
**poser lui-même** `app.societe_id` et `app.role`, c'est-à-dire **s'attribuer une
autorité que personne ne lui a accordée**. Ce n'est pas un contournement de RLS —
la politique est satisfaite — c'est une **auto-habilitation**, et c'est
exactement la classe de décision que le protocole réserve à l'exploitation.

#### La proposition, sur les quatre points demandés

**1. Le mot de passe initial ne transite nulle part.** Le geste n'en pose
**aucun**. Il ouvre l'identité et son habilitation, puis émet un **jeton de
premier accès** — une ligne de `verification`, à usage unique et datée, dont
l'identifiant opaque est le seul secret. Il est remis **hors bande** (l'URL est
imprimée une fois par le script, l'exploitation la transmet par son canal). Rien
n'entre au dépôt, rien n'entre au journal : `verification` n'est lisible que sous
désignation, et la valeur imprimée n'est pas relisible ensuite. *C'est la même
mécanique que les codes de secours de l'enrôlement, qui ne se relisent pas non
plus.*

**2. La trace.** `utilisateur` n'étant pas auditée, la trace doit être **écrite
par le geste**, pas espérée. Deux options, et je ne tranche pas :
*(a)* une valeur de plus à `EvenementAcces` — `ouverture_amorcage` — et une ligne
de `journal_acces` portant l'auteur (l'exploitation), l'horodatage, la société et
le compte ; *(b)* faire entrer `utilisateur` au périmètre d'audit, ce qui est
**un changement de I1** et bien au-delà de ce geste. *(a)* est le geste, *(b)*
est une question qui mérite la sienne.

**3. Le refus de servir deux fois.** Il existe déjà : `utilisateur.email` est
unique. Le geste n'a rien à inventer — il doit seulement **ne pas rattraper**
l'erreur d'unicité. Et une seconde borne, plus forte, est disponible sans rien
créer : *le geste refuse si la société porte DÉJÀ une habilitation quelconque*.
Il n'ouvre donc que la **première** identité d'une société, jamais la deuxième.

**4. Le critère de retrait — le point le plus important.** *Le jour où le chemin
administratif d'ouverture de compte existe, ce geste disparaît.* Ce critère est
**gardable**, et c'est ce qui le distingue d'une intention : un gardien statique
qui exige que le script d'amorçage n'existe plus dès qu'un appel à `signUpEmail`
apparaît **hors de lui et hors des tests**. La porte se referme le jour où la
porte principale s'ouvre, et c'est la machine qui le constate.

**Ce que je ne propose pas, et pourquoi :** aucune console d'administration —
ce serait le lot 7 par la bande, et l'exploitation l'a exclu.

### Q2 — L7-04 : dépendance nommée, non construit

Inconstruisible tant qu'aucun `admin_societe` ne peut se connecter, c'est-à-dire
tant que Q1 n'est pas tranchée. La dépendance est **écrite** : le déclencheur
`second_facteur_escalade` laisse déjà passer la remise à zéro dès que la date
d'expiration quitte la sentinelle ; **ce qui manque est le DROIT**, et aucune
politique ne l'accorde. Un compte parvenu à l'escalade — trente codes faux — n'a
aucun chemin de sortie.

---

## 4 — Ce qui n'a PAS été fait, et pourquoi

*(rien encore)*
